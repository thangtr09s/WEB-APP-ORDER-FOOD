import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  form_control: {
    width: "100%",
    paddingVertical: 8,
  },

  form_input: {
    marginVertical: 12,
    padding: 10,
    borderWidth: 1,
    borderRadius: 8,
  },
});
